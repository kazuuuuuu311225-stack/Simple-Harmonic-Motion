# 単振動の速度・加速度

**github.com の画面では動きません。** プログラムの中身が見えるだけです。

動かすには、このリポジトリの **Settings → Pages** で
Branch を `main`、Folder を `/ (root)` にして Save します。
1〜2分後に出る `https://（ユーザー名）.github.io/（リポジトリ名）/` が、
生徒に配るリンクです。授業用は末尾に `?lesson=1` を付けます。

